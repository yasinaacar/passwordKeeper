<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>
    <div class="container mt-3">
        <div class="row">
            <div class="col-md-12 mt-3">
                <h3 class="text-center my-5"><%= title %></h3>
                <form method="post">
                    <div class="row mb-3 d-flex justify-content-center align-items-center" style="height: 50vh;">
                        <%- include('../partials/alert') %>
                        <label for="categoryName" class="col-sm-2 col-form-label">Category Name :</label>
                        <div class="col-sm-8">
                            <input class="form-control" type="text" placeholder="Write Category Name" id="categoryName" name="categoryName" value="<%= (typeof values != "undefined") ? values.categoryName : "" %>">
                        </div>
                        <div class="col-sm-2 d-flex">
                            <button type="submit" class="btn greenThemeBtn ms-3">Create</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <%- include('../partials/script') %>
</body>
</html>